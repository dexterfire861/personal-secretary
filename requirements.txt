# Personal Secretary — runtime dependencies (Python 3.13)
# Local model + embeddings are served by Ollama and reached over plain urllib
# (no client library needed).
fastapi==0.137.1
uvicorn==0.49.0
pydantic==2.13.4
mcp==1.28.0
chromadb==1.5.9
